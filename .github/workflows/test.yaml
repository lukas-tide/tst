name: Testing
on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test outputs
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::debug::Set the Octocat variable"
          echo "::notice file=README.md,line=1,endLine=2,title=title::message"
          echo "::setOutput::Test Output"
          echo '# Testing echo to summary! 🚀' >> $GITHUB_STEP_SUMMARY
